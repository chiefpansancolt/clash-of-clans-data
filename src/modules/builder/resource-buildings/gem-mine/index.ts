import rawData from '@/data/builder/resource-buildings/gem-mine.json';
import { BuilderResourceBuilding } from '@/types';

export const gemMineData: BuilderResourceBuilding = rawData as unknown as BuilderResourceBuilding;
