import rawData from '@/data/home/hero-equipment/earthquake-boots.json';
import { HeroEquipment } from '@/types';

export const earthquakeBootsData: HeroEquipment = rawData as unknown as HeroEquipment;
