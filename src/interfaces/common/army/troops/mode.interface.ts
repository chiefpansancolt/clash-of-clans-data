interface Mode {
    preferredTarget: string
    unitTypeTarget: string
    attackType?: string
    effectType?: string
    movementSpeed: number
    attackSpeed?: number
    healSpeed?: number
    range: number
    damageRadius?: number
}

export default Mode
